# Required in production — change to a long random string
JWT_SECRET=cottage-food-portal-secret-key-change-in-production

# Optional — enables AI features (intake parsing, bio generation)
OPENAI_API_KEY=

# Optional — deployment
NEXT_PUBLIC_APP_URL=http://localhost:3000
