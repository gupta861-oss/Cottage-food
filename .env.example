# Copy this file to .env and fill in your own values. .env is gitignored --
# never commit real tokens.

# Required for scripts/apify_ingest.py (not needed for --dry-run or for
# running the Flask app itself). Get one from https://apify.com after
# creating a free account.
APIFY_API_TOKEN=
